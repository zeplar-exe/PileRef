using System.IO;
using System.Text;

namespace PileRef.Model.Document;

public class SvgDocument : TextDocumentBase
{
    public SvgDocument(DocumentUri uri, Encoding encoding) : base(uri, encoding)
    {
        
    }
}